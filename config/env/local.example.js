'use strict';

// Rename this file to local.js for having a local configuration variables that
// will not get commited and pushed to remote repositories.
// Add your Amazon MWS credentials to the fields below

/* For example:

module.exports = {
  accessKeyId: '*****', 
  secretAccessKey: '*****', 
  merchantId: '*****'
};

*/
